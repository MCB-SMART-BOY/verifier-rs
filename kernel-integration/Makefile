# SPDX-License-Identifier: GPL-2.0

# Rust BPF Verifier kernel module Makefile
#
# This module is built entirely in Rust using the kernel's
# native Rust support (no C glue code required).

obj-$(CONFIG_BPF_VERIFIER_RUST) += rust_bpf_verifier.o
